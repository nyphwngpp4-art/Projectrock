---
import type { Doctor } from '../data/doctors';

interface Props {
  doctor: Doctor;
}
const { doctor } = Astro.props;
const initials = doctor.name
  .split(' ')
  .filter((w) => w.length > 1)
  .map((w) => w[0])
  .join('');
---

<article class="flex h-full flex-col rounded-lg border border-sand-200 bg-white p-6 text-center transition-shadow hover:shadow-md hover:shadow-navy-100">
  <!-- Photography placeholder: swap for a real headshot; see ASSET-INVENTORY.md -->
  <div
    class="relative mx-auto flex h-28 w-28 items-center justify-center overflow-hidden rounded-full font-serif text-3xl text-sand-100"
    style="background: radial-gradient(115% 115% at 30% 20%, #2d557d 0%, #1b3350 55%, #14263c 100%);"
    role="img"
    aria-label={`Portrait placeholder for ${doctor.name}, ${doctor.degree}`}
  >
    <span class="absolute inset-[5px] rounded-full border border-white/20" aria-hidden="true"></span>
    {initials}
  </div>
  <h3 class="mt-4 font-serif text-xl font-semibold text-navy-900">
    {doctor.name}, {doctor.degree}
  </h3>
  <p class="mt-1 text-sm font-medium text-sage-700">{doctor.role}</p>
  <p class="mt-3 flex-1 text-[0.95rem] text-ink-soft">{doctor.cardLine}</p>
  <a
    href={`/doctors/${doctor.slug}/`}
    class="mt-4 font-semibold text-navy-700 hover:underline"
  >
    View profile<span class="sr-only"> for {doctor.name}, {doctor.degree}</span>
  </a>
</article>
